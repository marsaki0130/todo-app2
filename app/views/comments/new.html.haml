.container
  = form_with(model: @comment, url: task_comments_path(@task), method: 'post', local: true) do |f|
    %div
      = f.label :content, 'コメント'
    %div
      = f.text_area :content, class: 'board-area'
    = f.submit '保存' ,class: 'btn-primary'